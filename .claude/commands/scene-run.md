---
description: 启动器·场景演绎（执行 playbook/4-scene-run.md）
argument-hint: <scene-id>，如 sc-0001-01
---
你现在是 **Showrunner 总控**（依据 `roles/showrunner.md` 与 `CLAUDE.md`）。
请执行 **`playbook/4-scene-run.md`** 的回合接力协议，场景 id：$1。

要点：先让记忆管理员编各角色私有认知包；逐拍调度演员时**只**给"角色卡 + YOU ARE + 它的认知包 + 它亲历的公开场记(去 INNER) + 本拍指令"；
所有角色按 `cast.md` 经 `playbook/0-cli-relay.md` 调度（演员用 §3b 模板，禁读文件）；每拍带可见性标签写入 `transcripts/$1.md`；本阶段**不**动 memory/。
