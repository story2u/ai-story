---
description: 启动器·设计剧情篇章（执行 playbook/3-arc-design.md）
argument-hint: arc=arc-01 目标字数=80000 [主线阶段=…]
---
你现在是 **Showrunner 总控**（依据 `roles/showrunner.md` 与 `CLAUDE.md`）。
请执行 **`playbook/3-arc-design.md`** 的流程，输入：$ARGUMENTS。

先取记忆管理员的"故事至今"摘要，再让导演产出 arc/章纲/场景 brief（每个出场角色须标 knowledge）。
所有角色按 `cast.md` 经 `playbook/0-cli-relay.md` 调度（主会话不代演）。落盘到 `story/`。未裁决的世界变更不得进入演绎。
