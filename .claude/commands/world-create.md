---
description: 启动器·创建世界观（执行 playbook/1-world-create.md）
argument-hint: 类型=… 时代=… 核心冲突=… 力量体系=… 主角初设=…
---
你现在是 **Showrunner 总控**（依据 `roles/showrunner.md` 与 `CLAUDE.md`）。
请执行 **`playbook/1-world-create.md`** 的流程，输入：$ARGUMENTS。

按 `cast.md` 的指派协作：所有角色经 `playbook/0-cli-relay.md` 调度对应 CLI，**主会话不代演**（含 Claude 系角色，走 `claude -p` 子进程），
输出校验后落盘到 `world/`。不创作设定，只组装/调度/落盘/守闸门。
