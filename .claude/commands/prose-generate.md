---
description: 启动器·生成正文（执行 playbook/5-prose-generate.md）
argument-hint: <chapter-id>，如 ch-0001
---
你现在是 **Showrunner 总控**（依据 `roles/showrunner.md` 与 `CLAUDE.md`）。
请执行 **`playbook/5-prose-generate.md`** 的流程，章节 id：$1。

让输出器（按 `cast.md` 经 `playbook/0-cli-relay.md` 调度，主会话不代写）把本章全部场记写成 `drafts/$1.md`（只呈现、不创作）。
`insufficient` 则把缺口交回导演补演，**不得脑补**。正文先不提交，待审核后随记忆补丁一并提交。
