# R3 字段清理 · sc-0002-02 Beat 1 柯九

r2 内容方向可用，但仍需修正：
- `FLAG` 必须留空，除非你拒演；不要写 `continue`。
- `STATE_DELTA` 不得写“记录新出现的流言”，因为本拍尚未获得任何流言；只能写“准备记录/侦查焦点转向茶摊”。

请重新输出同一拍，严格只用七字段，不要加粗，不要代码块：

SPEAK:
ACT:
INNER:
BASIS:
STATE_DELTA:
KNOWLEDGE_GAINED:
FLAG:

保留 r2 的起手意图：柯九含蓄提“昨日清册风波”“旧契房规矩”“碑林旧事”，递出话头，不让 NPC 回答，不拿到线索。
