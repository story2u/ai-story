# R2 格式纠正

r1 内容方向可用，但字段名写成了 `**SPEAK:**` 这类 Markdown 加粗格式。请重新输出同一拍，严格只用下列七个原样字段名，不要加粗、不要代码块、不要寒暄：

SPEAK:
ACT:
INNER:
BASIS:
STATE_DELTA:
KNOWLEDGE_GAINED:
FLAG:

保留本拍意图：柯九用和气闲话钩出沈砚为何熟悉旧契房，不直接替沈砚争理，不替镇差动手，不暴露身份/修为/真实任务。
